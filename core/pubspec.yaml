name: core
description: Core module with localization, themes, utilities
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  easy_localization: ^3.0.8
  get_it: ^7.6.7
  equatable: ^2.0.5

  flutter_dotenv: ^5.1.0
  logger: ^2.0.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  easy_localization_generator: ^0.3.5
  build_runner: ^2.4.8

flutter:
  assets:
    - resources/lang/