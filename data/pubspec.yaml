name: data
description: Data module with repository implementations
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  domain:
    path: ../domain
  supabase_flutter: ^2.5.0
  drift: ^2.34.2
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.3
  path: ^1.9.0
  shared_preferences: ^2.2.3
  get_it: ^7.6.7
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^8.1.0
  share_plus: ^10.1.4
  flutter_svg: ^2.0.9
  crypto: ^3.0.6
  saver_gallery: ^4.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^2.14.0
  build_runner: ^2.4.8