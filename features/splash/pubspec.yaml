name: splash
description: Splash feature with authentication check
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  gallery:
    path: ../gallery

dev_dependencies:
  flutter_test:
    sdk: flutter

  auto_route_generator: ^10.6.0
  build_runner: ^2.4.8