name: gallery
description: Gallery feature
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  navigation:
    path: ../../navigation
  flutter_bloc: ^8.1.4
  equatable: ^2.0.5
  get_it: ^7.6.7
  cached_network_image: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  auto_route_generator: ^10.6.0
  build_runner: ^2.4.8